<Page x:Class="UltimateWardrobe.App.Views.OverhaulView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:UltimateWardrobe.App.ViewModels"
      xmlns:views="clr-namespace:UltimateWardrobe.App.Views"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      Title="Overhaul">

  <Page.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVis" />
    <views:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis" />
    <views:StringToSymbolConverter x:Key="Symbol" />
    <Style x:Key="MatrixCellCard" TargetType="Button">
      <Setter Property="Margin" Value="2" />
      <Setter Property="Padding" Value="10,8" />
      <Setter Property="HorizontalAlignment" Value="Stretch" />
      <Setter Property="VerticalAlignment" Value="Stretch" />
      <Setter Property="MinWidth" Value="160" />
      <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
      <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    </Style>
    <Style x:Key="MatrixRowName" TargetType="TextBlock">
      <Setter Property="MaxWidth" Value="220" />
      <Setter Property="TextWrapping" Value="Wrap" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="VerticalAlignment" Value="Center" />
      <Setter Property="Margin" Value="0,0,12,0" />
      <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    </Style>
    <!-- Default TextBlock foreground ensures black-on-dark never occurs even if theme not yet applied or on Light -->
    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    </Style>
  </Page.Resources>

  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Empty state -->
    <StackPanel Grid.Row="0"
                Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}">
      <TextBlock Text="Overhaul" FontSize="24" FontWeight="SemiBold" />
      <TextBlock Text="{Binding EmptyMessage}"
                 Margin="0,16,0,0"
                 Opacity="0.6" />
    </StackPanel>

    <StackPanel Grid.Row="0"
                Visibility="{Binding IsEmpty, Converter={StaticResource InverseBoolToVis}}">
      <TextBlock Text="{Binding OverhaulName, FallbackValue=Overhaul}"
                 FontSize="24" FontWeight="SemiBold" />
      <TextBlock Text="{Binding ProgressLabel}"
                 Margin="0,4,0,0"
                 Opacity="0.7" FontSize="12" />

      <!-- Status legend (Sprint 6.6 polish): a WPF-UI symbol + text per matrix status, no emoji -->
      <ItemsControl ItemsSource="{Binding StatusLegend}"
                    Margin="0,10,0,0">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <StackPanel Orientation="Horizontal" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
              <ui:SymbolIcon Symbol="{Binding Symbol, Converter={StaticResource Symbol}}"
                             Foreground="#FF5BC0EB"
                             Margin="0,0,5,0" />
              <TextBlock Text="{Binding Label}" FontSize="12" Opacity="0.85" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            </StackPanel>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>

      <StackPanel Orientation="Horizontal" Margin="0,16,0,12">
        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=250}"
                 Width="260"
                 VerticalContentAlignment="Center" />
        <ComboBox ItemsSource="{Binding StatusOptions}"
                  SelectedItem="{Binding SelectedStatusOption}"
                  Width="160"
                  Margin="12,0,0,0"
                  VerticalAlignment="Center" />
      </StackPanel>

      <!-- Fixed header row (aligns with the per-row name column + weight columns) -->
      <Grid ColumnDefinitions="232,*">
        <TextBlock Text="Armor set"
                   FontWeight="SemiBold"
                   FontSize="12"
                   Opacity="0.7" />
        <ItemsControl Grid.Column="1" ItemsSource="{Binding Columns}">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <StackPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate DataType="{x:Type vm:MatrixColumnViewModel}">
              <TextBlock Text="{Binding Header}"
                         Width="160" Margin="2,0"
                         FontWeight="SemiBold"
                         FontSize="12"
                         Opacity="0.7" />
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </Grid>
    </StackPanel>

    <!-- Matrix body: flat virtualized list of section headers + row bands (Sprint 6.8 rework, Phase B fix).
         Previously a ScrollViewer wrapping ItemsControl with VirtualizingStackPanel - which defeats
         virtualization because an explicit outer ScrollViewer gives infinite available height. Phase B
         replaces it with a ListView that owns its internal ScrollViewer, so VirtualizingStackPanel
         only realizes on-screen rows (manual-testing bug 3). Top margin keeps the vertical slider
         clear of the window close button (bug 4). Selection chrome is suppressed so custom row
         templates keep their visuals and ActivateCellCommand still routes; keyboard navigation via
         ListView selection is intentionally disabled. -->
    <ListView x:Name="MatrixListView"
              Grid.Row="2"
              Margin="0,20,0,0"
              Visibility="{Binding IsEmpty, Converter={StaticResource InverseBoolToVis}}"
              ItemsSource="{Binding MatrixItems}"
              Background="Transparent"
              BorderThickness="0"
              Padding="0"
              Focusable="True"
              IsTabStop="True"
              PreviewMouseWheel="OnListPreviewMouseWheel"
              ScrollViewer.CanContentScroll="True"
              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
              ScrollViewer.VerticalScrollBarVisibility="Auto"
              VirtualizingPanel.IsVirtualizing="True"
              VirtualizingPanel.VirtualizationMode="Recycling"
              VirtualizingPanel.ScrollUnit="Pixel"
              SelectionMode="Single">
      <ListView.ItemsPanel>
        <ItemsPanelTemplate>
          <VirtualizingStackPanel />
        </ItemsPanelTemplate>
      </ListView.ItemsPanel>
      <ListView.ItemContainerStyle>
        <Style TargetType="ListViewItem">
          <Setter Property="Background" Value="Transparent" />
          <Setter Property="BorderThickness" Value="0" />
          <Setter Property="Padding" Value="0" />
          <Setter Property="Margin" Value="0" />
          <Setter Property="HorizontalContentAlignment" Value="Stretch" />
          <Setter Property="VerticalContentAlignment" Value="Stretch" />
          <Setter Property="FocusVisualStyle" Value="{x:Null}" />
          <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
              <Setter Property="Background" Value="Transparent" />
              <Setter Property="BorderBrush" Value="Transparent" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="Transparent" />
              <Setter Property="BorderBrush" Value="Transparent" />
            </Trigger>
          </Style.Triggers>
        </Style>
      </ListView.ItemContainerStyle>
      <ListView.Resources>
        <DataTemplate DataType="{x:Type vm:MatrixSectionHeaderViewModel}">
          <TextBlock Text="{Binding Header}"
                     Margin="0,14,0,6"
                     FontWeight="SemiBold"
                     FontSize="16"
                     Opacity="0.9" />
        </DataTemplate>
        <!-- The row name is a clickable button (manual-testing bug 6): clicking an item opens the
             popover editor for its default (set, gender, weight) coordinate even before any mapping
             exists. The name wraps instead of truncating (manual-testing bug 5). -->
        <DataTemplate DataType="{x:Type vm:ArmorSetRowViewModel}">
          <Grid ColumnDefinitions="232,*">
            <Button Click="MatrixRow_Click"
                    Command="{Binding DataContext.ActivateCellCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                    CommandParameter="{Binding DefaultCell}"
                    Background="Transparent"
                    BorderThickness="0"
                    Padding="0,4,0,4"
                    HorizontalContentAlignment="Left"
                    ToolTip="{Binding DisplayName}">
              <TextBlock Text="{Binding DisplayName}"
                         Style="{StaticResource MatrixRowName}" />
            </Button>
            <ItemsControl Grid.Column="1" ItemsSource="{Binding Cells}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:MatrixCellViewModel}">
                  <Button Style="{StaticResource MatrixCellCard}"
                          Click="MatrixCell_Click"
                          Visibility="{Binding IsBlank, Converter={StaticResource InverseBoolToVis}}"
                          Command="{Binding DataContext.ActivateCellCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                          CommandParameter="{Binding}">
                    <ItemsControl ItemsSource="{Binding Lines}">
                      <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                          <StackPanel />
                        </ItemsPanelTemplate>
                      </ItemsControl.ItemsPanel>
                      <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:CellLineViewModel}">
                          <TextBlock Text="{Binding Text}"
                                     FontSize="12"
                                     Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                     TextTrimming="CharacterEllipsis" />
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>
                  </Button>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </Grid>
        </DataTemplate>
      </ListView.Resources>
    </ListView>

  </Grid>
</Page>
