<Window x:Class="UltimateWardrobe.App.Views.CellEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:UltimateWardrobe.App.ViewModels"
        xmlns:views="clr-namespace:UltimateWardrobe.App.Views"
        mc:Ignorable="d"
        Title="{Binding CellEditor.Title, FallbackValue=Replacement editor}"
        Width="720" Height="680"
        MinWidth="660" MinHeight="540"
        MaxWidth="960" MaxHeight="760"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}">

  <Window.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVis" />
    <views:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis" />
  </Window.Resources>

  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Header with title, status and Import patch - OS title bar provides the single close button -->
    <Grid Grid.Row="0" Margin="0,0,0,12">
      <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <TextBlock Text="{Binding CellEditor.Title}" FontWeight="SemiBold" FontSize="16" />
        <TextBlock Text="{Binding CellEditor.SetStatus}" Margin="12,0,0,0" VerticalAlignment="Center" FontSize="12" Opacity="0.7" />
        <Button Content="Import patch" Margin="12,0,0,0" Command="{Binding CellEditor.ImportPatchCommand}" />
      </StackPanel>
    </Grid>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
      <Grid ColumnDefinitions="280,*">
        <!-- ARMOR 1 - read-only target inventory -->
        <StackPanel Grid.Column="0" Margin="0,0,16,0">
          <TextBlock Text="ARMOR 1" FontWeight="SemiBold" FontSize="12" Opacity="0.8" />
          <TextBlock Text="Set to replace" FontSize="11" Opacity="0.6" Margin="0,2,0,10" />
          <ItemsControl ItemsSource="{Binding CellEditor.Rows}" Grid.IsSharedSizeScope="True">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <StackPanel />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate DataType="{x:Type vm:PieceInventoryRowViewModel}">
                <Border BorderBrush="#22FFFFFF" BorderThickness="0,0,0,1" Padding="0,6">
                  <StackPanel>
                    <StackPanel Orientation="Horizontal">
                      <TextBlock Text="{Binding EditorId}" FontWeight="SemiBold" FontSize="12" />
                      <TextBlock Text="{Binding Status}" Margin="8,0,0,0" VerticalAlignment="Center" FontSize="11" Opacity="0.7" />
                    </StackPanel>
                    <TextBlock Text="{Binding Slot}" FontSize="11" Opacity="0.7" />
                    <TextBlock Text="{Binding TargetMesh}" FontSize="11" Opacity="0.6" TextTrimming="CharacterEllipsis" />
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                      <ComboBox ItemsSource="{Binding AvailableDonorsForRow}" SelectedItem="{Binding SelectedDonor, Mode=TwoWay}" Width="170">
                        <ComboBox.ItemTemplate>
                          <DataTemplate DataType="{x:Type vm:DonorOption}">
                    <StackPanel Orientation="Horizontal">
                      <TextBlock Text="{Binding DisplayName}" Width="110" TextTrimming="CharacterEllipsis" />
                      <TextBlock Text="{Binding BodyMarkerText}" FontSize="10" Opacity="0.7" Margin="6,0,0,0" />
                      <TextBlock Text="3BA" FontSize="10" Margin="4,0,0,0" Foreground="#FF7BC67E" Visibility="{Binding HasThreeBA, Converter={StaticResource BoolToVis}}" />
                      <TextBlock Text="HIMBO" FontSize="10" Margin="4,0,0,0" Foreground="#FF7BA7C6" Visibility="{Binding HasHimbo, Converter={StaticResource BoolToVis}}" />
                      <TextBlock Text="HDT-SMP" FontSize="10" Margin="4,0,0,0" Foreground="#FFC67E7B" Visibility="{Binding HasPhysics, Converter={StaticResource BoolToVis}}" />
                    </StackPanel>
                          </DataTemplate>
                        </ComboBox.ItemTemplate>
                      </ComboBox>
                      <Button Content="Apply" Margin="6,0,0,0" Command="{Binding AssignDonorCommand}" Padding="8,2" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                      <TextBlock Text="{Binding DonorBodyMarkerText, StringFormat='Body: {0}'}" FontSize="11" Opacity="0.7" />
                      <TextBlock Text="BodySlide" FontSize="11" Opacity="0.7" Margin="8,0,0,0" Visibility="{Binding HasBodySlide, Converter={StaticResource BoolToVis}}" />
                      <TextBlock Text="Physics" FontSize="11" Opacity="0.7" Margin="8,0,0,0" Visibility="{Binding HasPhysics, Converter={StaticResource BoolToVis}}" />
                    </StackPanel>
                  </StackPanel>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </StackPanel>

        <!-- ARMOR 2 - the replacement mod -->
        <StackPanel Grid.Column="1" Margin="16,0,0,0">
          <TextBlock Text="ARMOR 2" FontWeight="SemiBold" FontSize="12" Opacity="0.8" />
          <TextBlock Text="Replacement mod" FontSize="11" Opacity="0.6" Margin="0,2,0,10" />
          <TextBlock Text="{Binding CellEditor.CurrentDonorText}" FontWeight="SemiBold" FontSize="13" />
          <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
            <ComboBox ItemsSource="{Binding CellEditor.AvailableDonors}" SelectedItem="{Binding CellEditor.SelectedDonor}" MinWidth="220">
              <ComboBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:DonorOption}">
                  <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding DisplayName}" Width="130" TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding BodyMarkerText}" FontSize="10" Opacity="0.7" Margin="6,0,0,0" />
                    <TextBlock Text="3BA" FontSize="10" Margin="4,0,0,0" Foreground="#FF7BC67E" Visibility="{Binding HasThreeBA, Converter={StaticResource BoolToVis}}" />
                    <TextBlock Text="HIMBO" FontSize="10" Margin="4,0,0,0" Foreground="#FF7BA7C6" Visibility="{Binding HasHimbo, Converter={StaticResource BoolToVis}}" />
                    <TextBlock Text="HDT-SMP" FontSize="10" Margin="4,0,0,0" Foreground="#FFC67E7B" Visibility="{Binding HasPhysics, Converter={StaticResource BoolToVis}}" />
                  </StackPanel>
                </DataTemplate>
              </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Content="{Binding CellEditor.LoadDonorLabel}" Margin="8,0,0,0" Command="{Binding CellEditor.LoadDonorCommand}" />
          </StackPanel>
          <StackPanel Margin="0,18,0,0">
            <TextBlock Text="{Binding CellEditor.BodyCheckText}" FontWeight="SemiBold" FontSize="12" />
            <StackPanel Orientation="Horizontal" Margin="0,6,0,0" Visibility="{Binding CellEditor.ShowBodyPatchRow, Converter={StaticResource BoolToVis}}">
              <ComboBox ItemsSource="{Binding CellEditor.BodyPatches}" SelectedItem="{Binding CellEditor.SelectedBodyPatch}" DisplayMemberPath="DisplayName" MinWidth="180" />
              <Button Content="{Binding CellEditor.LoadBodyPatchLabel}" Margin="8,0,0,0" Command="{Binding CellEditor.LoadBodyPatchCommand}" />
            </StackPanel>
            <Button Content="{Binding CellEditor.ClearBodyPatchLabel}" Margin="0,6,0,0" HorizontalAlignment="Left" Visibility="{Binding CellEditor.ShowClearBodyPatch, Converter={StaticResource BoolToVis}}" Command="{Binding CellEditor.ClearBodyPatchCommand}" />
            <TextBlock Text="{Binding CellEditor.PhysicsCheckText}" FontWeight="SemiBold" FontSize="12" Margin="0,14,0,0" />
            <StackPanel Orientation="Horizontal" Margin="0,6,0,0" Visibility="{Binding CellEditor.ShowPhysicsPatchRow, Converter={StaticResource BoolToVis}}">
              <ComboBox ItemsSource="{Binding CellEditor.PhysicsPatches}" SelectedItem="{Binding CellEditor.SelectedPhysicsPatch}" DisplayMemberPath="DisplayName" MinWidth="180" />
              <Button Content="Load HDT-SMP patch" Margin="8,0,0,0" Command="{Binding CellEditor.LoadPhysicsPatchCommand}" />
            </StackPanel>
            <Button Content="Clear HDT-SMP patch" Margin="0,6,0,0" HorizontalAlignment="Left" Visibility="{Binding CellEditor.ShowClearPhysicsPatch, Converter={StaticResource BoolToVis}}" Command="{Binding CellEditor.ClearPhysicsPatchCommand}" />
          </StackPanel>
        </StackPanel>
      </Grid>
    </ScrollViewer>
  </Grid>
</Window>
